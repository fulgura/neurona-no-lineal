clear all;
clc;

T = [ -1 -1	1 1;
    -1 1 -1 1];

funcion = 'logsig';

feval_aplicada = feval ( funcion, T );


funcion = 'tansig';
n = -4:0.1:4; 
plot(n);
a = feval ( funcion, n ); 
plot(n,a)

feval()


